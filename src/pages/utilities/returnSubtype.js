const returnSubtype = (subtype) => {
    if (subtype === "offer") {
        return "post"
    }
    else if (subtype = "post") {
        return "post"
    }
    else {
        return subtype
    }
}

export default returnSubtype;